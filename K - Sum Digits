#include<stdio.h>
#include<string.h>
int main()
{
    int n,i,sum=0;
    char arr[1000000]= {};
    scanf("%d",&n);
    scanf("%s",&arr);
    for(i=0; i<n; i++)
    {
        sum=sum+(arr[i]-48);
    }
    printf("%d",sum);
    return 0;
}
